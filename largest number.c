//WAP TO FIND THE LARGEST NUMBER USING THE LOGICAL 'AND' operator

#include<stdio.h>
int main()
{
    int a,b,c;
    printf("the value of a , b , c \n");
    scanf("%d %d %d",&a , &b , &c);
    if(a>b && a>c){
        printf("a is greatest");
    }
    else if(b>a && b>c){
        printf("b is greatest");
    }
    else if(c>a && c>b){
        printf("c is greatest");
    }
    else if(
            printf("all are equal")
    )
        return 0;
}
